# Memory: index.md
Updated: now

# Project Memory

## Core
Sempre que houver mudanças no banco (Supabase), exibir o SQL COMPLETO direto no chat (em bloco ```sql) para o usuário copiar, ALÉM de salvar em arquivo `SUPABASE_*.sql` na raiz do projeto. Nunca apenas referenciar o arquivo — sempre colar o conteúdo no chat.
