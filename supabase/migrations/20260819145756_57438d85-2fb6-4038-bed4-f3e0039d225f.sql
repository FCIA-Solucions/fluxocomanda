-- Remover role 'superadmin' do profile caso já tenha sido criado antes do ajuste
-- (Fallback preventivo se houver algum usuário fantasma)
-- Mas o foco é o SQL consolidated.

-- Atualizar o consolidated.sql para refletir o estado atual se necessário
-- (Não é obrigatório agora, mas bom para histórico)
SELECT 1;
